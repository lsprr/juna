{
    "name": "juna",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"cd backend && npm run start\" \"cd frontend && npm start\" "
    },
    "devDependencies": {
        "concurrently": "^7.2.2",
        "nodemon": "^2.0.18"
    }
}